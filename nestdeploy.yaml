node: '20'

database:
  type: postgresql
  migrateCommand: 'node node_modules/.bin/typeorm migration:run -d dist/data-source.js'

services:
  redis: true

healthCheck:
  path: '/health'
  initialDelaySeconds: 15
  periodSeconds: 10
